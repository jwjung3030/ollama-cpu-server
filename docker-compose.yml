services:
  ollama:
    image: ollama/ollama:latest
    container_name: ollama-server
    deploy:
      resources:
        reservations:
          devices:
            - capabilities: [gpu]
    volumes:
      - ./ollama:/root/.ollama
    ports:
      - "11434:11434"
